import { OfficeLocationCatalogManagementScreen } from '@/components/office-location-catalog-management-screen';

/** Шаблоны локаций для любого офиса — выбор офиса через селектор. */
export default function AdminWorkerOfficeLocationCatalogScreen() {
  return <OfficeLocationCatalogManagementScreen variant="admin-worker" />;
}
